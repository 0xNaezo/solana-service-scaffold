[advisories]
ignore = [
    # RUSTSEC-2023-0071: RSA Marvin Attack.
    # Ignored because it is pulled in via sqlx-mysql, which is disabled by features.
    # Proof: `cargo tree -i sqlx-mysql` returns nothing.
    "RUSTSEC-2023-0071",
]
